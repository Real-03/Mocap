namespace BeatStyleGame
{
    /// <summary>
    /// Indica com que membro do corpo o jogador deve interagir com o cubo
    /// (ex: acertar com a mão, ou tocar/pisar com o pé).
    /// </summary>
    public enum TipoMembro
    {
        MaoEsquerda,
        MaoDireita,
        PeEsquerdo,
        PeDireito
    }
}
